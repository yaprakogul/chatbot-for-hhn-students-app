def answer_with_rag(user_query: str) -> str:
    return ("(RAG stub) I’d answer this using the booklet/FAQ knowledge base. "
            "We’ll wire this tomorrow. For now, try a route-style question like "
            "‘How do I go from A to Mensa?’")
